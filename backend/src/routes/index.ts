const routes = (app) => {
    /* GET */
    app.get("/", (req, res) => {
        res.send("Food Delivery Backend running!");
    });

    /* POST */
    /* PUT */
    /* DELETE */
};

export default routes;
